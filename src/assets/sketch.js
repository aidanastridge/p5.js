function setup() {
  // Creates a drawing canvas on the webpage
  createCanvas(400, 400); 
}

function draw() {
  // Fills the background with a light grey color
  background(220); 
  
  // Draws a circle in the center of the canvas
  ellipse(200, 200, 50, 50); 
}